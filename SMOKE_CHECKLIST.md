# 배포 후 점검 체크리스트

RLS(접근 통제)를 켠 뒤에는 **권한이 없으면 에러 대신 "빈 목록"이 나온다.**
그래서 화면이 열리는 것만으로는 통과가 아니다. **반드시 실제 데이터가 보이는지**로 판단할 것.

> ⚠️ **빈 목록이면 통과가 아니다.**
> "학생이 없습니다", "기록이 없습니다", 표에 아무 줄도 없음 → 전부 **실패**로 간주하고 아래 [문제가 생기면] 절로 갈 것.

점검은 **관리자 → 학생 → 학부모** 순서로 한다. 각 항목 옆 `[ ]`에 체크.

---

## 0. 사전 준비

- [ ] 브라우저를 **새로고침**했다 (Ctrl+F5 / Cmd+Shift+R)
- [ ] 기존에 로그인돼 있었다면 **로그아웃 후 다시 로그인**했다
      → 이번 배포는 세션 형식이 바뀌어서 **전원 재로그인이 필요**하다. 비밀번호는 그대로다.
- [ ] 브라우저 개발자도구 콘솔을 열어 두었다 (F12 → Console). 빨간 에러가 뜨면 캡처.

---

## 1. 관리자 계정

로그인: 관리자 전화번호 + 비밀번호

- [ ] **로그인 성공** — 로그인 화면이 사라지고 대시보드가 뜬다
- [ ] **대시보드** — 재원 학생 수가 0이 아니고, 학생 현황 표에 **학생 이름이 실제로 보인다**
- [ ] **학생 관리** — 학생 카드가 보이고, 이름·학교·목표 대학이 채워져 있다
- [ ] **학생 관리 → 저장** — 아무 학생의 학교명을 고쳤다가 되돌리고 [저장] → "저장되었습니다" 표시
- [ ] **상담 기록** — 학생 선택 시 기존 상담 내역이 보인다 (없는 학생이면 다른 학생으로 확인)
- [ ] **주간테스트 → 회차 목록** — 기존 회차가 보인다
- [ ] **주간테스트 → 채점 그리드** — 학생×문항 표가 뜨고 기존 점수가 채워져 있다
- [ ] **채점 그리드 → 태그 버튼**(문항 셀의 태그 아이콘) — 패널이 열리고 **오답 원인 버튼들(조건 해석·계산 실수 등)이 보인다**
      → 이 항목은 이번에 고친 부분이다. 버튼이 안 보이면 실패.
- [ ] **채점 그리드 → 사진 첨부** — 태그 패널에서 사진 파일을 올리고 [저장] → 실패 메시지가 없다
- [ ] **사진 다시 보기** — 같은 셀의 태그 패널을 다시 열면 **방금 올린 사진이 표시된다**
      ("사진 불러오는 중..."에서 멈추거나 "권한이 없습니다"가 뜨면 실패)
- [ ] **과제 점검** — 학생 선택 시 과제 기록과 그래프가 보인다
- [ ] **취약 진단** — 히트맵에 색칠된 칸이 보인다
- [ ] **첨삭 관리** — 첨삭 이력이 보인다. 오답 노트의 사진도 표시된다
- [ ] **합격 가능성** — 목표 대학 카드와 밴드(안정/적정/도전)가 보인다
- [ ] **진행 현황 리포트** — 표와 그래프가 보인다
- [ ] **대학 정보** — 대학 목록이 보인다 (16개 내외)
- [ ] **설정 → DB 마이그레이션 상태** — **"최신 상태"** 초록 배지
      → "미적용 N건"이면 해당 SQL을 아직 실행하지 않은 것이다
- [ ] **설정 → 비밀번호 변경** — 새 비밀번호로 바꾸고 → **로그아웃 후 새 비밀번호로 로그인 성공**
      (확인했으면 원래 비밀번호로 되돌려 두기)

---

## 2. 학생 계정

로그아웃 후, 학생 계정으로 로그인

- [ ] **로그인 성공**
- [ ] **내 리포트** — 본인 점수·그래프가 보인다
- [ ] **상담 내역** — **[학생·학부모에게 공개]** 를 체크해 저장한 상담만 보인다.
      → 관리자만 보게 한 상담이 **여기 보이면 심각한 문제**다. 즉시 보고.
      → 전사문·AI 초안이 보이면 **심각한 문제**다. 즉시 보고.
- [ ] **취약 진단** — 본인 히트맵이 보인다
- [ ] **첨삭 이력** — 본인 첨삭과 오답 노트가 보인다
- [ ] **목표 대학** — 본인 목표 대학 카드가 보인다
- [ ] **다른 학생 데이터가 섞여 보이지 않는다** — 화면 어디에도 본인 아닌 학생 이름이 없다
- [ ] 왼쪽 메뉴에 **[학생 관리]·[주간테스트]·[설정] 같은 관리자 메뉴가 없다**

---

## 3. 학부모 계정

로그아웃 후, 학부모 계정으로 로그인

- [ ] **로그인 성공** — 모바일용 단일 페이지가 뜬다
- [ ] **자녀 이름**이 맞게 표시된다
- [ ] **회차 점수·과제 정답률** 숫자가 `-`가 아니라 실제 값이다
- [ ] **주간 점수 추이 그래프**가 그려진다
- [ ] **단원별 정답률** 막대가 보인다
- [ ] **강사 코멘트**가 보인다(등록된 주차라면)
- [ ] **상담 기록** — **[학생·학부모에게 공개]** 를 체크해 저장한 상담만 보인다(유형과 무관하다).
      최근 3건이 펼쳐져 있고 나머지는 [지난 상담 N건 더 보기] 로 접혀 있다.
      → 공개용 문장을 채운 상담은 그 문장이, 비워 둔 상담은 상담 내용이 보인다.
      → 공개 체크를 하지 않은 상담·전사문·AI 초안이 보이면 **심각한 문제**다. 즉시 보고.
- [ ] **목표 대학 · D-day**가 보인다
- [ ] **다른 학생 정보가 보이지 않는다**

---

## 4. 데모 모드 (선택)

주소 뒤에 `?demo=1` 을 붙여 접속

- [ ] 로그인 없이 바로 대시보드가 뜬다
- [ ] 상단 미리보기 전환(관리자/학생/학부모)이 동작한다
- [ ] 데모 데이터(김민준·이서연 등)가 보인다

---

## 문제가 생기면

### 화면이 전부 빈 목록이다
가장 흔한 원인은 **순서 문제**다.
1. 로그아웃 → 다시 로그인 (새 토큰을 받아야 한다)
2. 그래도 비어 있으면 **SQL을 코드 배포보다 먼저 실행한 것**이다.
   → 되돌리려면 `db/migrations/0001_baseline.sql` 의 RLS 블록을 다시 실행하면 즉시 원상복구된다.

### "세션이 만료되었을 수 있습니다" 경고가 뜬다
로그아웃 후 다시 로그인.

### 사진이 안 보인다
1. `db/migrations/0007_storage_private.sql` 을 실행했는지 확인
2. 실행했다면 관리자로 로그인해 사진이 보이는지 확인
3. 예전에 올린 사진은 경로 형식이 달라 안 보일 수 있다 → 해당 사진만 다시 올리면 된다

### 로그인이 아예 안 된다
비밀번호가 틀린 게 아니라면 서버 설정 문제일 수 있다.
콘솔(F12)에 뜨는 메시지를 그대로 캡처해서 보고할 것.

---

**점검 완료일: __________  점검자: __________**
